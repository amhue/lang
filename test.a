fun add(a b)
{
    return a + b;
}

fun sub(a b)
{
    return a - b;
}

fun main()
{
    auto x;
    auto i32 y;
    x = 2 + 3;

    if (x == 2)
        x = add(x 1);
    else
        x = sub(x 2);

    return x;
}
